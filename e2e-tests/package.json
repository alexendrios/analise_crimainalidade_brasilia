{
  "name": "e2e-tests-criminalidade-brasilia",
  "version": "2.0.0",
  "description": "Testes End-to-End UI do Dashboard Criminalidade Brasilia/DF — CodeceptJS + Playwright + Cucumber + Allure + POM",
  "scripts": {
    "test:ui": "codeceptjs run --steps --grep @ui",
    "test:e2e": "codeceptjs run --steps",
    "test:smoke": "codeceptjs run --steps --grep @smoke",
    "test:regression": "codeceptjs run --steps --grep @regression",
    "test:all": "codeceptjs run --steps --plugins allure",
    "test:headed": "SHOW=true codeceptjs run --steps --grep @ui",
    "allure:attach": "node utils/attach-allure-evidence.js",
    "allure:serve": "npm run allure:attach && allure serve allure-results",
    "allure:report": "npm run allure:attach && allure generate allure-results --clean -o allure-report"
  },
  "dependencies": {
    "codeceptjs": "latest",
    "playwright": "latest"
  },
  "devDependencies": {
    "allure-codeceptjs": "^3.11.0",
    "allure-commandline": "latest",
    "allure-js-commons": "^3.11.0"
  }
}
